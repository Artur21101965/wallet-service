# ⚡ Быстрый деплой

## Способ 1: Railway (Рекомендуется - 2 минуты)

### Автоматический деплой через веб-интерфейс:

1. **Откройте https://railway.app** и войдите через GitHub

2. **Нажмите "New Project"** → **"Deploy from GitHub repo"**

3. **Выберите ваш репозиторий** (или создайте новый и загрузите код)

4. **Добавьте переменные окружения:**
   - Нажмите на проект → Settings → Variables
   - Добавьте все переменные из вашего `.env` файла:
     ```
     NETWORK=sepolia
     TOKEN_ADDRESS_SEPOLIA=ваш_адрес_контракта
     TOKEN_ADDRESS_MAINNET=ваш_адрес_контракта
     SPENDER_ADDRESS=0xE4576aC79aBbe431EdD7aA55111a843529285edB
     PRIVATE_KEY=ваш_приватный_ключ
     SEPOLIA_RPC_URL=ваш_rpc_url
     MAINNET_RPC_URL=ваш_rpc_url
     ```

5. **Дождитесь деплоя** (автоматически начнется)

6. **Получите URL** проекта (например: `https://wallet-service-production.up.railway.app`)

7. **Обновите BASE_URL:**
   - Settings → Variables
   - Добавьте: `BASE_URL=https://ваш-проект.railway.app`
   - Railway автоматически перезапустит проект

8. **Готово!** Используйте URL для QR кодов

---

## Способ 2: Render (Альтернатива)

1. **Откройте https://render.com** и войдите через GitHub

2. **New +** → **Web Service**

3. **Подключите репозиторий**

4. **Настройки:**
   - Name: `wallet-service` (или любое другое)
   - Environment: `Node`
   - Build Command: `npm install`
   - Start Command: `npm start`

5. **Добавьте переменные окружения** (Environment Variables)

6. **Create Web Service**

7. **После деплоя** обновите `BASE_URL` на реальный URL

---

## Способ 3: Через Railway CLI

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Инициализируйте проект
railway init

# Добавьте переменные
railway variables set NETWORK=sepolia
railway variables set TOKEN_ADDRESS_SEPOLIA=ваш_адрес
railway variables set SPENDER_ADDRESS=0xE4576aC79aBbe431EdD7aA55111a843529285edB
railway variables set PRIVATE_KEY=ваш_ключ
railway variables set SEPOLIA_RPC_URL=ваш_rpc
railway variables set MAINNET_RPC_URL=ваш_rpc

# Задеплойте
railway up

# После деплоя получите URL и установите BASE_URL
railway variables set BASE_URL=https://ваш-проект.railway.app
```

---

## ⚠️ Важно:

- **Не коммитьте `.env` файл!** Он уже в `.gitignore`
- **Приватный ключ** храните только в переменных окружения платформы
- **После деплоя обязательно** обновите `BASE_URL` на реальный URL
- **Проверьте работу:** откройте `https://ваш-домен/qr-generator.html`

---

## 🎯 После деплоя:

1. Откройте `https://ваш-домен/qr-generator.html`
2. Сгенерируйте QR код
3. Проверьте, что URL в QR коде правильный (должен быть ваш домен)
4. Готово! Теперь можно использовать QR коды везде

