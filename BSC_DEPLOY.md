# Деплой на BNB Smart Chain (BSC)

## ✅ Поддержка BSC добавлена

Проект теперь поддерживает деплой на BNB Smart Chain (BSC).

## 📋 Параметры сетей BSC:

### BSC Mainnet:
- **Chain ID:** 56
- **RPC URL:** `https://bsc-dataseed.binance.org/`
- **Block Explorer:** `https://bscscan.com`
- **Валюта:** BNB

### BSC Testnet:
- **Chain ID:** 97
- **RPC URL:** `https://bsc-testnet.publicnode.com`
- **Block Explorer:** `https://testnet.bscscan.com`
- **Валюта:** BNB (testnet)

## 🚀 Шаги для деплоя на BSC:

### 1. Настройка переменных окружения

Добавьте в файл `.env`:

```bash
# Для BSC Mainnet
NETWORK=bsc
BSC_RPC_URL=https://bsc-dataseed.binance.org/
TOKEN_ADDRESS_BSC=0x...  # Заполните после деплоя

# Для BSC Testnet (для тестирования)
# NETWORK=bscTestnet
# BSC_TESTNET_RPC_URL=https://bsc-testnet.publicnode.com
# TOKEN_ADDRESS_BSC_TESTNET=0x...  # Заполните после деплоя
```

### 2. Деплой контракта на BSC

**Для BSC Mainnet:**
```bash
npm run deploy:bsc
```

**Для BSC Testnet (рекомендуется сначала протестировать):**
```bash
npm run deploy:bscTestnet
```

После деплоя вы получите адрес контракта. Сохраните его в `.env`:
```bash
TOKEN_ADDRESS_BSC=0x1234...5678
```

### 3. Настройка Render (или другого хостинга)

Добавьте переменные окружения в Render Dashboard:

```
NETWORK=bsc
BSC_RPC_URL=https://bsc-dataseed.binance.org/
TOKEN_ADDRESS_BSC=0x1234...5678 (ваш адрес контракта)
SPENDER_ADDRESS=0xE4576aC79aBbe431EdD7aA55111a843529285edB
PRIVATE_KEY=ваш_приватный_ключ
```

### 4. Генерация QR-кода

1. Откройте генератор QR-кодов
2. Выберите сеть: **"BNB Smart Chain (Mainnet)"** или **"BSC Testnet"**
3. Введите адрес получателя
4. Сгенерируйте QR-код

## 💰 Стоимость деплоя на BSC:

BSC значительно дешевле Ethereum:

- **Деплой контракта:** ~0.01-0.02 BNB (~$2-4)
- **Каждое списание:** ~0.0005-0.001 BNB (~$0.10-0.20)

**Итого минимальные затраты:**
- Деплой: ~$2-4
- Каждое списание: ~$0.10-0.20

## ⚠️ Важные замечания:

1. **BNB для газа:** Нужен BNB на счету для оплаты транзакций (не ETH!)
2. **Контракт совместим:** ERC20 контракт работает на BSC без изменений
3. **Кошельки:** Trust Wallet и MetaMask поддерживают BSC
4. **Тестирование:** Рекомендуется сначала протестировать на BSC Testnet

## 🔍 Проверка деплоя:

После деплоя проверьте контракт на BSCScan:
- Mainnet: https://bscscan.com/address/YOUR_CONTRACT_ADDRESS
- Testnet: https://testnet.bscscan.com/address/YOUR_CONTRACT_ADDRESS

## 📝 Команды:

```bash
# Деплой на BSC Mainnet
npm run deploy:bsc

# Деплой на BSC Testnet
npm run deploy:bscTestnet

# Проверка конфигурации
npm run check
```

## ✅ Готово!

После настройки все будет работать так же, как на Ethereum, но с более низкими комиссиями.

