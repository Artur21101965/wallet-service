# 🔌 Настройка Render MCP для автоматического деплоя

## Что это даст:

Если подключить Render MCP сервер, я смогу автоматически:
- ✅ Создать Web Service на Render
- ✅ Добавить все переменные окружения
- ✅ Задеплоить проект
- ✅ Получить URL проекта
- ✅ Настроить BASE_URL

**Всё без ручного вмешательства!**

---

## Как настроить Render MCP в Cursor:

### Шаг 1: Получите Render API Key

1. Откройте https://dashboard.render.com
2. Перейдите в **Account Settings** → **API Keys**
3. Нажмите **"Create API Key"**
4. Дайте название (например: "Cursor MCP")
5. Скопируйте API ключ (он показывается только один раз!)

### Шаг 2: Настройте MCP в Cursor

1. Откройте настройки Cursor
2. Найдите раздел **MCP Servers** или **Model Context Protocol**
3. Добавьте новый сервер:

**Для Cursor (через settings.json):**

```json
{
  "mcpServers": {
    "render": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-render"
      ],
      "env": {
        "RENDER_API_KEY": "ваш_api_ключ_здесь"
      }
    }
  }
}
```

**Или через UI:**
- Server URL: `https://mcp.render.com/mcp`
- API Key: ваш Render API ключ

### Шаг 3: Перезапустите Cursor

После настройки перезапустите Cursor, чтобы MCP сервер подключился.

---

## После настройки:

Просто скажите мне: **"Задеплой проект на Render"** и я сделаю всё автоматически!

---

## Альтернатива: Ручной деплой

Если MCP не настроен, используйте инструкцию:
- `DEPLOY_RENDER_NOW.md` - пошаговая инструкция
- `QUICK_RENDER_DEPLOY.txt` - быстрый старт

---

## Полезные ссылки:

- **Render Dashboard:** https://dashboard.render.com
- **API Keys:** https://dashboard.render.com/account/api-keys
- **MCP Documentation:** https://render.com/docs/mcp

