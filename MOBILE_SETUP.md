# Настройка для мобильных устройств

## Проблема

QR код содержит `localhost:3000`, но мобильные устройства не могут подключиться к `localhost` вашего компьютера.

## Решение

### Вариант 1: Автоматическое определение IP (рекомендуется)

Сервер автоматически определяет IP адрес вашего компьютера в локальной сети и использует его в QR кодах.

При запуске сервера вы увидите:
```
🚀 Сервер запущен:
   Локально: http://localhost:3000
   В сети: http://192.168.1.100:3000
   Используйте этот адрес для QR кодов на мобильных устройствах
```

### Вариант 2: Указать IP вручную

1. Узнайте IP адрес вашего компьютера:
   ```bash
   # На Mac:
   ipconfig getifaddr en0
   
   # Или:
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

2. Добавьте в `.env` файл:
   ```
   SERVER_IP=192.168.1.100
   ```
   (замените на ваш IP адрес)

3. Перезапустите сервер

### Вариант 3: Указать IP в интерфейсе

1. Откройте `http://localhost:3000/qr-generator.html`
2. В поле "IP адрес сервера" введите IP вашего компьютера
3. Сгенерируйте QR код

## Проверка подключения

Убедитесь, что:
1. Компьютер и мобильное устройство в одной Wi-Fi сети
2. Файрволл не блокирует порт 3000
3. IP адрес правильный

## Если не работает

1. Проверьте, что сервер запущен
2. Проверьте IP адрес в консоли при запуске сервера
3. Попробуйте открыть `http://ВАШ_IP:3000` в браузере на компьютере
4. Если работает на компьютере, но не на мобильном - проверьте файрволл

