# 🚀 Деплой на Render (бесплатный план)

## Пошаговая инструкция:

### Шаг 1: Создайте GitHub репозиторий (если еще нет)

1. Откройте https://github.com/new
2. Название: `wallet-service` (или любое другое)
3. Создайте репозиторий (можно приватный)
4. Загрузите код:

```bash
git remote add origin https://github.com/ваш-username/wallet-service.git
git branch -M main
git push -u origin main
```

### Шаг 2: Регистрация на Render

1. Откройте https://render.com
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub** (рекомендуется)

### Шаг 3: Создание Web Service

1. В Dashboard нажмите **"New +"**
2. Выберите **"Web Service"**
3. Нажмите **"Connect account"** если нужно подключить GitHub
4. Выберите репозиторий `wallet-service`

### Шаг 4: Настройка сервиса

Заполните форму:

- **Name:** `wallet-service` (или любое другое)
- **Environment:** `Node`
- **Region:** Выберите ближайший (например, `Frankfurt`)
- **Branch:** `main` (или ваша основная ветка)
- **Root Directory:** (оставьте пустым)
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Plan:** **Free** ✅

### Шаг 5: Добавление переменных окружения

**ВАЖНО:** Добавьте переменные ДО создания сервиса!

В разделе **"Environment Variables"** нажмите **"Add Environment Variable"** и добавьте:

```
NETWORK = sepolia
TOKEN_ADDRESS_SEPOLIA = ваш_адрес_контракта
TOKEN_ADDRESS_MAINNET = ваш_адрес_контракта
SPENDER_ADDRESS = 0xE4576aC79aBbe431EdD7aA55111a843529285edB
PRIVATE_KEY = ваш_приватный_ключ
SEPOLIA_RPC_URL = ваш_rpc_url
MAINNET_RPC_URL = ваш_rpc_url
PORT = 10000
```

**Примечание:** Render автоматически устанавливает `PORT`, но лучше указать явно `10000`.

### Шаг 6: Создание сервиса

1. Нажмите **"Create Web Service"**
2. Render начнет автоматический деплой
3. Дождитесь завершения (обычно 2-5 минут)

### Шаг 7: Получение URL и настройка BASE_URL

1. После успешного деплоя Render покажет URL:
   - Например: `https://wallet-service.onrender.com`
2. Перейдите в **Settings** → **Environment**
3. Добавьте новую переменную:
   ```
   BASE_URL = https://wallet-service.onrender.com
   ```
   (замените на ваш реальный URL)
4. Render автоматически перезапустит сервис

### Шаг 8: Проверка работы

1. Откройте ваш URL: `https://wallet-service.onrender.com/qr-generator.html`
2. Сгенерируйте QR код
3. Проверьте, что URL в QR коде правильный (должен быть ваш домен Render)

---

## ✅ Готово!

Теперь ваш проект доступен по адресу Render и может использоваться везде.

---

## 🔧 Полезные команды и ссылки

- **Dashboard:** https://dashboard.render.com
- **Логи:** В Dashboard → ваш сервис → Logs
- **Переменные:** Settings → Environment
- **Перезапуск:** Manual Deploy → Clear build cache & deploy

---

## ⚠️ Важные моменты:

1. **Бесплатный план Render:**
   - Сервис "засыпает" после 15 минут неактивности
   - Первый запрос после простоя может занять 30-60 секунд
   - Это нормально для бесплатного плана

2. **Переменные окружения:**
   - Не коммитьте `.env` файл в Git
   - Все секреты храните только в Render Environment Variables

3. **Обновление кода:**
   - Просто сделайте `git push` в ваш репозиторий
   - Render автоматически задеплоит новую версию

---

## 🐛 Если что-то пошло не так:

1. **Проверьте логи:** Dashboard → ваш сервис → Logs
2. **Проверьте переменные:** Settings → Environment
3. **Перезапустите:** Manual Deploy → Clear build cache & deploy
4. **Проверьте Build Command и Start Command**

---

## 📝 Быстрая шпаргалка:

```
1. GitHub репозиторий ✅
2. Render.com → New + → Web Service
3. Подключить GitHub репозиторий
4. Настройки:
   - Build: npm install
   - Start: npm start
   - Plan: Free
5. Добавить переменные окружения
6. Create Web Service
7. Добавить BASE_URL после получения URL
8. Готово! ✅
```

