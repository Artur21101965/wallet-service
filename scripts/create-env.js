const fs = require('fs');
const path = require('path');

const envPath = path.join(__dirname, '..', '.env');
const envExamplePath = path.join(__dirname, '..', 'env.example');

console.log('📝 Создание .env файла с настройками...\n');

let envContent = `# ⚠️ ВНИМАНИЕ: Этот файл содержит конфиденциальную информацию
# Используйте только с тестовыми кошельками

# Порт сервера
PORT=3000

# IP адрес сервера для QR кодов (оставьте пустым для автоматического определения)
# Укажите IP вашего компьютера в локальной сети, например: 192.168.1.100
SERVER_IP=

# Текущая сеть (sepolia или mainnet)
NETWORK=sepolia

# Адрес контракта токена (ERC20 с поддержкой permit)
# Sepolia - заполните после развертывания контракта
TOKEN_ADDRESS_SEPOLIA=0x...
# Mainnet - заполните после развертывания контракта
TOKEN_ADDRESS_MAINNET=0x...

# Адрес получателя разрешения (ваш адрес для списания средств)
SPENDER_ADDRESS=0xE4576aC79aBbe431EdD7aA55111a843529285edB

# RPC URL для подключения к блокчейну
# Sepolia - публичный endpoint
SEPOLIA_RPC_URL=https://rpc.sepolia.org
# Mainnet - публичный endpoint
MAINNET_RPC_URL=https://eth.llamarpc.com

# Приватный ключ для списания средств (⚠️ НИКОГДА не коммитьте этот файл!)
PRIVATE_KEY=0xf2c2d9da30ec7da900c3b756ee6ceaabb337b111568448a46197c7b50669df2d
`;

try {
    fs.writeFileSync(envPath, envContent);
    console.log('✅ .env файл создан с настройками:');
    console.log('   - Приватный ключ: настроен');
    console.log('   - Адрес получателя: 0xE4576aC79aBbe431EdD7aA55111a843529285edB');
    console.log('   - Sepolia RPC: https://rpc.sepolia.org');
    console.log('   - Mainnet RPC: https://eth.llamarpc.com');
    console.log('   - Сеть по умолчанию: sepolia\n');
    console.log('⚠️  Осталось только развернуть контракт и добавить его адрес в .env\n');
} catch (error) {
    console.error('❌ Ошибка создания .env файла:', error.message);
    process.exit(1);
}

