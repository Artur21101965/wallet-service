# 📦 Создание GitHub репозитория

## Способ 1: Через веб-интерфейс (рекомендуется)

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. **Repository name:** `wallet-service` (или любое другое название)
3. **Description:** (опционально) Wallet service for permit signing
4. **Visibility:** 
   - ✅ Public (рекомендуется для Render)
   - или Private (если хотите приватный)
5. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите **"Create repository"**

### Шаг 2: Подключите локальный репозиторий

После создания GitHub покажет инструкции. Выполните:

```bash
# Удалите старый remote (если был)
git remote remove origin

# Добавьте правильный remote (ЗАМЕНИТЕ ваш-username на ваш GitHub username)
git remote add origin https://github.com/ваш-username/wallet-service.git

# Проверьте
git remote -v

# Загрузите код
git branch -M main
git push -u origin main
```

**Важно:** Замените `ваш-username` на ваш реальный GitHub username!

---

## Способ 2: Через GitHub CLI (если установлен)

```bash
# Проверьте авторизацию
gh auth status

# Если не авторизованы
gh auth login

# Создайте репозиторий и загрузите код
gh repo create wallet-service --public --source=. --remote=origin --push
```

---

## Способ 3: Вручную через терминал

Если у вас уже есть репозиторий на GitHub:

```bash
# 1. Удалите старый remote
git remote remove origin

# 2. Добавьте правильный URL (ЗАМЕНИТЕ на ваш реальный URL)
git remote add origin https://github.com/ваш-username/wallet-service.git

# 3. Проверьте
git remote -v

# 4. Загрузите код
git push -u origin main
```

---

## Проверка

После успешной загрузки:

1. Откройте https://github.com/ваш-username/wallet-service
2. Убедитесь, что все файлы загружены
3. Теперь можно деплоить на Render!

---

## Следующий шаг

После создания репозитория на GitHub:
1. Откройте https://render.com
2. Следуйте инструкциям из `DEPLOY_RENDER.md`

