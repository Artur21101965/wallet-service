# 🚀 ВЫПОЛНИТЕ ЭТУ КОМАНДУ ДЛЯ АВТОМАТИЧЕСКОГО ДЕПЛОЯ

## Вариант 1: Через npm (рекомендуется)

```bash
npm run deploy
```

Скрипт автоматически:
1. ✅ Проверит зависимости
2. ✅ Инициализирует git
3. ✅ Установит Railway CLI (если нужно)
4. ⚠️  Попросит авторизоваться в Railway (один раз)
5. ✅ Инициализирует проект
6. ✅ Загрузит переменные окружения из .env
7. ✅ Задеплоит проект
8. ✅ Получит URL и обновит BASE_URL

## Вариант 2: Через bash скрипт

```bash
./deploy.sh
```

## Вариант 3: Вручную (если автоматика не работает)

```bash
# 1. Установите Railway CLI (если еще не установлен)
npm install -g @railway/cli

# 2. Авторизуйтесь (откроется браузер)
railway login

# 3. Запустите автоматический деплой
npm run deploy
```

---

## ⚠️ Важно перед деплоем:

1. **Убедитесь, что файл `.env` заполнен** со всеми необходимыми переменными
2. **Проверьте, что все значения корректны** (адреса контрактов, RPC URLs, приватный ключ)

---

## 📝 Что происходит при деплое:

1. Скрипт читает все переменные из `.env`
2. Автоматически создает Railway проект
3. Загружает все переменные в Railway
4. Деплоит код
5. Получает URL проекта
6. Обновляет `BASE_URL` автоматически

---

## ✅ После деплоя:

Откройте URL, который покажет скрипт, например:
- `https://ваш-проект.railway.app/qr-generator.html`

---

## 🔧 Если что-то пошло не так:

1. Проверьте, что вы авторизованы: `railway whoami`
2. Проверьте логи: `railway logs`
3. Проверьте переменные: `railway variables`
4. Перезапустите: `railway restart`

