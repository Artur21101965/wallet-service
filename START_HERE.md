# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

Откройте терминал в этой папке и выполните:

```bash
npm install --legacy-peer-deps
```

Эта команда установит все зависимости, игнорируя конфликты версий между ethers 5 и hardhat-toolbox.

Если возникнут проблемы с правами, попробуйте:
```bash
sudo npm install --legacy-peer-deps
```

## Шаг 2: Создание .env файла

```bash
cp env.example .env
```

Или создайте файл `.env` вручную и скопируйте содержимое из `env.example`.

## Шаг 3: Настройка RPC URLs

Откройте `.env` файл и замените `YOUR_KEY` на ваши ключи:

### Получение ключей:

**Infura (рекомендуется):**
1. Зайдите на https://infura.io
2. Зарегистрируйтесь или войдите
3. Создайте новый проект
4. Скопируйте API ключ
5. Замените в `.env`:
   ```
   SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/ВАШ_КЛЮЧ
   MAINNET_RPC_URL=https://mainnet.infura.io/v3/ВАШ_КЛЮЧ
   ```

**Alchemy (альтернатива):**
1. Зайдите на https://alchemy.com
2. Зарегистрируйтесь
3. Создайте новое приложение
4. Скопируйте API ключ
5. Замените в `.env`:
   ```
   SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/ВАШ_КЛЮЧ
   MAINNET_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/ВАШ_КЛЮЧ
   ```

## Шаг 4: Развертывание контракта в Sepolia

```bash
npm run deploy:sepolia
```

После выполнения команды вы увидите адрес контракта. Скопируйте его и добавьте в `.env`:

```
TOKEN_ADDRESS_SEPOLIA=0x...  # Вставьте адрес из вывода команды
```

## Шаг 5: Проверка конфигурации

```bash
npm run check
```

Эта команда покажет, что еще нужно настроить.

## Шаг 6: Запуск сервера

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## ✅ Готово!

Теперь вы можете:
- Открыть `http://localhost:3000/qr-generator.html` для генерации QR кодов
- Открыть `http://localhost:3000/admin.html` для управления permit

## 📝 Уже настроено:

- ✅ Приватный ключ: `0xf2c2d9da30ec7da900c3b756ee6ceaabb337b111568448a46197c7b50669df2d`
- ✅ Адрес получателя: `0xE4576aC79aBbe431EdD7aA55111a843529285edB`
- ✅ Сеть по умолчанию: Sepolia

## 🔧 Полезные команды:

- `npm run setup` - показать инструкции
- `npm run check` - проверить конфигурацию
- `npm run deploy:sepolia` - развернуть в Sepolia
- `npm run deploy:mainnet` - развернуть в Mainnet
- `npm start` - запустить сервер
- `npm run dev` - запустить в режиме разработки

## ⚠️ Важно:

- Файл `.env` содержит конфиденциальную информацию - не коммитьте его в git!
- Используйте только с тестовыми кошельками
- Для Mainnet развертывайте только когда будете готовы

